# EXEC — 2026-08-10-2026-08-10-readme-harness-provenance

## v1
Changed files:
- `README.md` — added a "Provenance, for the time budget" note directly after
  the "Why a system, not just two workflows" subsection: the `.claude/`
  harness is reused/copy-pasted from another project, not written from
  scratch here; this project's root `CLAUDE.md` rules took ~2 minutes to
  describe. No new heading added, per R2/A3 — it reads as the closing
  paragraph of the existing subsection.

STATE: stage=EXECUTED, next_actor=Validator, exec_version=1
