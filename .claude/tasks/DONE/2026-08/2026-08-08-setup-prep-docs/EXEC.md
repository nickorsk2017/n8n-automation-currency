# EXEC — 2026-08-08-setup-prep-docs
# EXEC — 2026-08-08-setup-prep-docs

## v1
Changed files:
- README.md (new)

Setup section covers R1 (freecurrencyapi account, Docker n8n, LLM
provider account, env vars referencing `.env.example`), R2 (secrets via
`.env` -> n8n credentials, never in workflow JSON), R3 (screenshots kept
out of repo), R4 (optional Notion MCP mirror, framed as non-functional
tooling), R5 (stubbed Data Table schema / system prompt / trade-offs
sections marked pending). No workflow/, docs/, or screenshots/ files
touched.

STATE: stage=EXECUTED, next_actor=Validator, exec_version=1
